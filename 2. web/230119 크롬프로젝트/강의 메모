 #### 총 복습
 백그라운드 배경, a tag로 배경
 1. 이미지를 가져올때 절대경로와 상대경로가 있음
 -절대경로는 / 로 시작
 -불편함을 해소하기 위해 ./ 상대경로를 추천
 2. 태그 (각각의 역할, 사용법을 아주 잘 알아야함)
 -input
 -hi
 -시멘틱 테그
 -등등
3. css 
-적용법 
스타일 속성으로 적용하는건 추천하지 않음(inline)
스타일 테그로 적용
파일을 분리해서 링크로 불러오는 것을 가장 추천(내컴터에 있는거 뿐만 아니라 외부의 css 가져오는 것도 가능. 부트스트렙)
링크는 헤더안에
스크립트는 바디 바로 위에
4. 선택자
- 태그 선택자 - 여러개 사용 가능
- 클래스 명(.찍고) - 여러개 사용 가능
- 아이디(#찍고)- 아이디는 하나만 써야함
5. 글자 색
- color
6. element 배경색
-backgroundcolor
7. txt 정렬
- textalign
- 양쪽정렬은?
8. 
- flex 쓰고 align
9. 
10. 디스플레이
- block(영역의 한줄 전체)크기변경 가능 ex div
- inline(쓰여진것만 차지)크기변경 불가 ex span, input
11. position
- 옮길때 사용
- relative 부모를 기준으로 상대적인 위치 선정
- absolute static 아닌 부모를 기준으로 배정
- fixed 고정시킬때
- static 아무것도 지정하지 않은경우
12. display
- flex 
13. z index
- 화면의 요소가 겹쳐보일때 순서를 결정하는 역할
14. 자바와 자바스크립트는 상관없음
15. 자바스크립트와 j는 상관이 깊음
16. 자바스크립트 타입 종류
- 어레이
- 넘버
- 참조형인지 원시형인지
17. var let const
- 재선언이 가능한지
- 차이
18. == vs === 
- 타입까지 비교, 값까지 비교
19. 오브젝트 연습
20. dom 가져오려면 document.queryselector
21. queryselector
- 도메인에 있는것을 가져오는것?
22. addeventlistener
23. textcontext
- 테그안에 있는 문자열
24. innerHTML은 오답으로 할거임

25. classlist 5가지
26. 부트스트랩은 브라우저에서 제공하지않기 때문에 ,,

